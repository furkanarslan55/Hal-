@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewBag.Title - qualite</title>
    <link rel="stylesheet" href="~/css/site.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
</head>

<body class="d-flex flex-column min-vh-100 bg-light">

    <!-- HEADER -->
    <header class="shadow-sm sticky-top"
            style="background-image: url('@Url.Content("~/images/halıkedi.jpg")');
               background-size: cover;
               background-position: center;
               background-repeat: no-repeat;
                
               color: black;">

        <div class="container py-4 text-center">
            <div class="text-center mb-3">
                <div class="p-0 mb-2 bg-dark text-white mx-auto"
                     style="font-size: 2rem; color: ; letter-spacing: 30px; font-family:'Times New Roman', Times, serif; width: 500px;">
                    qualite
                </div>
               
            </div>
        </div>>
        </div>
            <!-- Menü -->
        <nav class="bg-dark">
            <ul class="nav justify-content-center">
                <li class="nav-item">
                    <a class="nav-link fw-bold" href="#" style=" font-family: 'Courier New', monospace;color:white;font-size:25px; ">Markalar</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link fw-bold" href="#" style=" font-family: 'Courier New', monospace;color:white;font-size:25px;">Biz Kimiz</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link fw-bold" href="#" style=" font-family: 'Courier New', monospace;color:white;font-size:25px;">Sosyal Medya</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link fw-bold" href="#" style=" font-family: 'Courier New', monospace;color:white;font-size:25px;">İletişim</a>
                </li>
            </ul>
        </nav>

        </div>
    </header>

    <!-- SAYFA İÇERİĞİ -->
    <main class="flex-grow-1">
        <div class="container py-4">
            @RenderBody()
        </div>
    </main>

    <!-- FOOTER -->
    <footer class="bg-white border-top mt-auto">
        <div class="container py-4 d-flex flex-column flex-md-row justify-content-between align-items-center">
            <div>
                <a href="#" class="fw-semibold text-decoration-none text-dark">Markalarımız</a>
            </div>
            <div>
                <a href="#" class="fw-semibold text-decoration-none text-dark">Koleksiyonlarımız</a>
            </div>
            <div>
                <a href="#" class="fw-semibold text-decoration-none text-dark">Ürünlerimiz</a>
            </div>
        </div>
        <div class="text-center small text-muted pb-3">
            © @DateTime.Now.Year qualite. Tüm hakları saklıdır.
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
